<?php
	// define pdo object parameters
	$dsn = 'mysql:host=localhost;dbname=pokedex';
	$username = 'b3llydrum';
	$password = 'Bittie90';

	// create pdo object
	$db = new PDO($dsn, $username, $password);

	// return all sql requests as UTF-8
	$db->exec("SET CHARACTER SET utf8");

	// simple printout test
	if(!$db)
		print("<strong>There was an error connecting to the database.\n</strong>");



?>
